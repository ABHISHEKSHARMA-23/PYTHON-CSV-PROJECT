# Python Employee CSV Filter

A simple Python tool that reads employee data from a CSV file and filters records based on a specific department.

## 🚀 Features
* Reads data from a `.csv` file using Python's built-in `csv` module.
* Filters employees based on the **Department** column.
* Prints matching records to the terminal.

## 📂 Project Structure
* `filtering_csv.py`: The main Python script.
* `emp_details.csv`: The data file containing employee IDs, names, and departments.

## 🛠️ How to Use
1. Clone this repository or download the files.
2. Ensure you have Python installed.
3. Place `emp_details.csv` in the same folder as the script.
4. Run the script using the terminal:
   ```bash
   python "filtering csv.py"
